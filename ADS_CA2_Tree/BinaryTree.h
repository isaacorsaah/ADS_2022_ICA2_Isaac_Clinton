#pragma once
#include "TNode.h"
#include <vector>

template <typename K, typename E>
class BinaryTree
{
	//void insertItem(TNode<T>* node, int& pos, int* arr);
public:
	E* search(K key) {
		return nullptr;
	}
};
